{% extends 'base.html.twig' %}

{% block title %}Edit Car
{% endblock %}

{% block body %}
    <h1>Modifier une voiture</h1>

    {{ include('car/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('car_index') }}">Retour à la liste</a>

    {{ include('car/_delete_form.html.twig') }}
{% endblock %}
