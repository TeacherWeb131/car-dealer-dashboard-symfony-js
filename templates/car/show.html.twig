{% extends 'base.html.twig' %}

{% block title %}Car
{% endblock %}

{% block body %}
    <h1>Car</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ car.brand }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ car.model }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ car.category }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ car.year }}</td>
            </tr>
            <tr>
                <th>Milage</th>
                <td>{{ car.milage }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ car.price }}
                    €</td>
            </tr>
            <tr>
                <th>Owner</th>
                <td>{{ car.owner.firstName }}
                    {{ car.owner.lastName }}
                </td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning btn-sm" href="{{ path('car_index') }}">back to list</a>

    <a class="btn btn-success btn-sm" href="{{ path('car_edit', {'id': car.id}) }}">edit</a>

    {{ include('car/_delete_form.html.twig') }}
{% endblock %}
